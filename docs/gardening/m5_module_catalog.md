# M5 Module Catalog (snapshot)

Quick map of Miaou modules to guide gardening (API consistency, `.mli` coverage, dedup work). Generated by listing `src/` and flagging `.ml` files without a sibling `.mli`.

## Widget families

- **Display** (`src/miaou_widgets_display`)
  - With `.mli`: `card_widget`, `sidebar_widget`, `toast_widget`, `tabs_widget`, `breadcrumbs_widget`, `link_widget`, `validated_textbox_widget`, `pane_layout`, `vsection`.
  - Missing `.mli`: `description_list.ml`, `pager_widget.ml`, `palette(.ml/.sdl.ml)`, `table_widget(.ml/.sdl.ml)`, `tree_widget.ml`, `widgets.ml`.
- **Layout** (`src/miaou_widgets_layout`)
  - With `.mli`: `flex_layout`, `pane_layout`, `sidebar_widget`, `card_widget`, `toast_widget`, `vsection`.
  - Missing `.mli`: `file_browser_widget.ml`, `list_nav.ml`, `progress_widget(.sdl).ml`, `spinner_widget(.sdl).ml`.
- **Input** (`src/miaou_widgets_input`)
  - With `.mli`: `button_widget`, `checkbox_widget`, `radio_button_widget`, `switch_widget`, `validated_textbox_widget`.
  - Missing `.mli`: `select_widget(.sdl).ml`, `textbox_widget.ml`.
- **Navigation** (`src/miaou_widgets_navigation`)
  - With `.mli`: `tabs_widget`, `breadcrumbs_widget`, `link_widget`.
- **Feedback** (`src/miaou_widgets_feedback`)
  - With `.mli`: `toast_widget`.

## Core / drivers / runner

- **Core** (`src/miaou_core`): missing `.mli` for `lambda_term_driver.ml`, `html_driver.ml`, `sdl_driver.ml`, `lib_tui.ml`, `narrow_modal.ml`, `quit_flag.ml`, `logger_alias.ml`, `sdl_enabled.ml`.
- **Drivers**: term (`src/miaou_driver_term/lambda_term_driver.ml`), SDL (`src/miaou_driver_sdl/sdl_driver.ml`, `sdl_enabled.ml`) lack `.mli`.
- **Runner** (`src/miaou_runner`): `html_driver.ml`, `runner_cli.ml`, `runner_native.ml`, `runner_tui.ml`, `tui_driver_common.ml`, `miaou_runner_*_main.ml` missing `.mli`.
- **Internals**: `src/lib_miaou_internal/flash_bus.ml`, `flash_toast_renderer.ml`, `headless_driver.ml` missing `.mli`.
- **Top-level**: `miaou.ml`, `cohttp_net.ml` lack `.mli`.

## Notes for gardening (M5_90)
- Track missing `.mli` count: 40 files without signatures; prioritize public-facing widgets (table, pager, select, textbox) and core drivers.
- Consider shared helper extraction for display/layout rendering once `.mli` stubs are in place (palette/widgets/table share render helpers).
- Keep this catalog refreshed when modules are added or `.mli` files land.
